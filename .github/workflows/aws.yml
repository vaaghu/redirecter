name: Deploy to Amazon ECS

on:
  push:
    branches: [ "main" ]
    
permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: redirectRunner
    
    steps:
    
    - name: Checkout
      uses: actions/checkout@v3
      
    - name: install packages
      run:
        npm ci
